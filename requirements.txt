# Core LangGraph / LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-google-genai>=2.0.0
langchain-anthropic>=0.3.0
langsmith>=0.1.0

# Google Sheets & Drive
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0
gspread>=6.0.0

# Meta Ads API
facebook-business>=20.0.0

# HTTP / GraphQL (Fireflies)
httpx>=0.27.0
gql>=3.5.0

# Data validation
pydantic>=2.0.0

# Scheduling
schedule>=1.2.0

# Knowledge Base — vector store + embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0   # multilingual-e5-base (French/Darija/English)
google-generativeai>=0.8.0     # Gemini embeddings (text-embedding-004)

# PDF extraction (optional — for Drive PDF files)
pdfminer.six>=20221105

# Google Docs API
google-api-python-client>=2.0.0  # already listed above, ensures docs.v1 endpoint

# Utilities
python-dotenv>=1.0.0
tenacity>=8.0.0
structlog>=24.0.0
rich>=13.0.0
pandas>=2.0.0
